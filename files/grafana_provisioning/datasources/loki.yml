apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    jsonData:
      derivedFields:
        - datasourceUid: runner
          matcherRegex: "runner"
          name: Runner
          url: "$${__value.raw}"
